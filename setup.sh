#!/bin/bash

# Change directory to dir1 and run npm install
cd melon-client
npm install

# Change back to the previous directory
cd ..

# Change directory to dir2 and run npm install
cd melon-server
npm install
