// config.ts
// NOTE: Change this to point to your actual nodeJS server IP/DNS
// if hosting on EC2 instance or something....
const API_BASE_URL = 'http://localhost:5000';

const config = {
  API_BASE_URL,
};

export default config;